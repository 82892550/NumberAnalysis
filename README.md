# NumberAnalysis
NumberAnalysis is a C++ program that analyzes a set of numbers and provides advanced statistics.
The program performs the following tasks:

Finds the maximum and minimum numbers in the set.

Counts the prime numbers.

Counts the palindrome numbers (numbers that read the same forwards and backwards).

Finds the number with the maximum number of divisors.
